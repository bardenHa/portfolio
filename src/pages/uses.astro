---
import { Divider, MdxComponents, Typography } from '@/lib/components';
import Page from '@/lib/layouts/page.astro';
import { getEntry } from 'astro:content';

const uses = await getEntry('pages', 'uses');
const { Content } = await uses.render();
---

<Page
  title="Uses"
  description="These are the various tools I use that add notable value to me and make me more productive."
  keywords="uses, tools, productivity, gear, software, hardware, apps, services, tech, tech stack, stack, setup, setup tour, desk setup, go, golang, typescript, javascript, ts, js, zsh, oh my zsh, starship, vscode, visual studio code, vim, neovim"
  image="/images/gear.webp"
  type="website"
>
  <section class="mt-12">
    <Typography.Display>Uses 🧰</Typography.Display>
    <Typography.Paragraph size="lg" variant="subdued" class="mt-8">
      These are the various tools I use that add notable value to me and make me more productive.
    </Typography.Paragraph>
  </section>
  <Divider size="lg" />
  <article>
    <Content components={MdxComponents} />
  </article>
</Page>
